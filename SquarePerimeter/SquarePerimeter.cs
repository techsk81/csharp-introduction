using System;
using System.Collections.Generic;
using System.Text;

namespace SquarePerimeter
{
    class SquarePerimeter
    {
        static void Main(string[] args)
        {
            int side = 4;

            int perimeter = 4 * (side);

            Console.WriteLine("Calculated perimeter of the sqaure is: " + perimeter);
        }
    }
}
